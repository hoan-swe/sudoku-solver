class unsolved_hash_table:
    def __init__(self, grid):
      self.table = {}
      for i in range(9):
        for j in range(9):
          if grid[i][j] == 0:
            self.table[str(i)+str(j)] = set()

    def look_up(self, row, col):
        return self.table[str(row)+str(col)]

    def set_possible_nums(self, nums_set, row, col):
        self.table[str(row)+str(col)] = nums_set